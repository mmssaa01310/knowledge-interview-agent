FROM python:3.12-slim

WORKDIR /workspace/app/voice

COPY app/voice /workspace/app/voice

RUN pip install --no-cache-dir setuptools \
    && pip install --no-cache-dir --no-build-isolation -e .

CMD ["uvicorn", "ai_interviewer_voice.main:app", "--host", "0.0.0.0", "--port", "8010", "--reload"]
